# Python
__pycache__/
*.py[cod]
.pytest_cache/

# ESPHome / firmware build artifacts
.esphome/
*.bin
build/

# Secrets (never commit)
secrets.yaml
*.key

# Local rollback copy of the live device config (carries the inline WiFi password)
firmware/pool-bridge-original.yaml

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
